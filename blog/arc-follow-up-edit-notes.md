# Edit Notes: ARC Follow-Up Draft

## Overall

The draft had a clear thesis but repeated the same distinction several times: `AGENTS.md` as rules, `brain/` as knowledge, skills as workflows. The updated version keeps that distinction but gives each section one job.

Recommended screenshot count: 3. More than that will slow the post down.

## Title Options

Best options:

1. `So I Started ARC and realised the agent needs a brain`
2. `I stopped stuffing everything into AGENTS.md`
3. `From skills to a brain: what changed after Deep Past`

Option 1 is best for continuity with the first post. Option 2 is sharper and more clickable. Option 3 is clearest but less personal.

## Intro

Purpose: connect to the previous post and state the new thesis.

What changed: integrated the Codex move, ARC link, and stronger-agent caveat without making the post about model rankings.

No screenshot needed here.

## The Old Pattern

Purpose: explain the problem with giant instruction files.

What to watch: avoid overexplaining the old `CLAUDE.md`/`AGENTS.md`. The reader only needs to know it mixed rules and repo documentation.

Screenshot candidate:

- A cropped old instruction-file table of contents or heading list.
- Redact private paths, hostnames, account names, run IDs, and any internal infra.
- The screenshot should visually show "this file became too many things."
- Include only a heading outline if possible; redact private hostnames, paths, accounts, and run IDs.

## What Changed in ARC

Purpose: introduce the new architecture.

What changed: added the Karpathy/Nous context and merged the autonomy section into this argument.

Screenshot candidate:

- Side-by-side crop:
  - left: current `AGENTS.md` headings;
  - right: `brain/index.md` with Concepts/Queries visible.
- This replaced most of the explanatory bullets in the tightened draft.

## Skills Are Procedures. The Brain Is Memory.

Purpose: make the conceptual distinction from the first blog post.

This section adds something new. Keep it.

Screenshot candidate:

- Optional. A simple screenshot of available skills next to `brain/SCHEMA.md` may help, but this is less important than the AGENTS/brain screenshot.

## Concrete Example

Purpose: ground the abstract workflow in the Deep Past reproduction repo.

What changed: replaced file-path list repetition with source-family examples and kept the public/private artifact point.

Screenshot candidate:

- Best screenshot: published brain query page `What Makes the Data Pipeline Distinctive?`
- It should show the query title and a few main differentiator bullets.
- This screenshot replaces the old file-path list.

## The Smaller AGENTS.md Is the Point

Status: removed as a standalone section in the tight draft.

Reason: the layer table now appears in `The New Pattern`, so the standalone section repeated the thesis.

## What I Trust the Agent With Now

Purpose: keep the post grounded. This prevents the piece from sounding like "just add a wiki and trust the agent."

Compressed from two bullet lists into two short paragraphs.

No screenshot needed.

## Ending

Purpose: tie back to the original claim.

What changed: removed repeated "manual competing is over" framing from earlier sections and saved it for the end.

No screenshot needed.
